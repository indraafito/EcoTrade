# Example environment variables for EcoTrade (Vite)
# Copy this file to `.env.local` or `.env` and replace placeholders with real values.
# Do NOT commit your real `.env.local`/`.env` that contains secrets.

# Supabase
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-public-anon-key

# Optional: If your app uses a service role key anywhere (server-side only), add it to your server env, NOT frontend.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
